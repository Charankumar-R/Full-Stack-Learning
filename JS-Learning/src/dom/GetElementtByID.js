const taskInput = document.getElementById("taskInput");
const addBtn = document.getElementById("addBtn");
const taskList = document.getElementById("taskList");

/*
    Here we’re grabbing elements from the HTML using their id.
    document.getElementById("taskInput") → gets the <input> box.
    document.getElementById("addBtn") → gets the "Add" button.
    document.getElementById("taskList") → gets the <ul> where tasks will be shown.
*/