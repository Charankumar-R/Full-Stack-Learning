function addTask() {
  const taskText = taskInput.value.trim();

/*
We read what the user typed in the input (taskInput.value).

.trim() removes spaces at the start/end (so " study " becomes "study").

📖 DOM Concept:

.value → gets the current text inside an <input> element.

*/
